<%= form_for @item, :as => :item, :url => {:action => :update_users_groups}, :html => {:method => :put, :id => 'itemForm'} do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form_users_groups', :locals => {:f => f} %>
  <%= button_tag '戻る', :type => :button, :onclick => "document.location='#{url_for(:action => :index)}';" %>
  <%= f.submit 'この内容で登録する' %>
<% end %>
