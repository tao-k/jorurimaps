<table class="show">
  <tr>
    <th><%= @item.locale(:name) %></th>
    <td><%= @item.name %></td>
  </tr><tr>
    <th><%= @item.locale(:retired_at) %><%= required %></th>
    <td>
      <%= f.date_select :retired_at, :use_month_numbers => true, :start_year => Time.now.year, :end_year => Time.now.year+10 %>
      <%= check_box_tag :immediately %><%= label_tag :immediately, '即時に退職させる' %>
    </td>
  </tr>
</table>
